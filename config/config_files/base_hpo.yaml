id: "base_hpo"
base_config: 'base'
hpo:
  num_samples: 50
  cpu_per_trial: 4
  gpu_per_trial: 0.2
  hpo_algorithm:
    name: 'BayesOptSearch'
    params:
      mode: 'max'
optimizer:
  name: 'Adam'
  params:
    lr: ['loguniform', [0.0001, 0.005]]
    weight_decay: ['loguniform', [0.00001, 0.001]]